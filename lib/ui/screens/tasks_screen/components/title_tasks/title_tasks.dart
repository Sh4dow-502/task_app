import 'package:flutter/material.dart';

class TitleTasks extends StatelessWidget {
  const TitleTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Design",
      style: TextStyle(
        fontSize: 30,
        fontWeight: FontWeight.w500,
      ),
    );
  }
}
